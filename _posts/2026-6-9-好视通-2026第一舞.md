---
layout: post
date: 2026-6-9
title: "好视通-2026第一舞"
author: "XinYiSleep"
category: Java
---
<h1 id="mWxxc">一.基础信息</h1>


<h2 id="MB93N">1.1: 题外话</h2>
```
先讲讲我自己吧，大半年没更新了有朋友问我java还更吗？我说更的因为我身体的原因导致这大半年没更新，
我经常拉肚子去医院做的肠镜和胃镜，发现胃里有个息肉好在不是什么大问题，师傅们还是需要注意身体。
```

<h2 id="MB93">1.2：安装和调试</h2>
```
通过网盘分享的文件：FastMeeting_Ent_ServerV4.30.5.4.exe
链接: [https://pan.baidu.com/s/1ZoEozpofXXEQ8_LC6i_lRQ](https://pan.baidu.com/s/1ZoEozpofXXEQ8_LC6i_lRQ) 提取码: dpvm 
--来自百度网盘超级会员v4的分享
安装还是蛮简单的，下一步下一步就是了，之后打开tomcat修改start.bat文件，修改调试关机重启就ok了。
rem start "javawebserver" /D.\ "..\jre\bin\javawebserver" -Djava.util.logging.config.file=".\conf\logging.properties" -Djava.util.logging.manager=org.apache.juli.ClassLoaderLogManager -Djava.endorsed.dirs=".\endorsed" -classpath "..\jre\lib;.\bin\bootstrap.jar;.\bin\tomcat-juli.jar" -Dcatalina.base="." -Dcatalina.home="." -Djava.io.tmpdir=".\temp" org.apache.catalina.startup.Bootstrap start
..\jre\bin\javawebserver -agentlib:jdwp=transport=dt_socket,server=y,suspend=n,address=5005 -Djava.util.logging.config.file=".\conf\logging.properties" -Djava.util.logging.manager=org.apache.juli.ClassLoaderLogManager -Djava.endorsed.dirs=".\endorsed" -server -Xms2g -Xmx2g -XX:+UseG1GC -XX:MetaspaceSize=256M -classpath "..\jre\lib;.\bin\bootstrap.jar;.\bin\tomcat-juli.jar" -Dcatalina.base="." -Dcatalina.home="." -Djava.io.tmpdir=".\temp" org.apache.catalina.startup.Bootstrap start
```


<h1 id="mWxxssc">二.鉴权</h1>
```
这个系统是典型的ssm框架，鉴权还是满有意思的只能说，这里讲一个技巧就是鉴权特别多的时候懒得一个一个找，
在可以调试的清空下，我们可以在页面找一个可以访问的路由找到路由断点，看堆栈一个一个看就行还是非常方便的，
我们就用好视通举个例子，图一中找一个能访问的路由，之后反编译代码搜索这个路由/login/validateCode.do图二，
下面就是断点看看咯，图三，还是比较实用的，在就是请求出现302等跳转的时候，直接找HttpServletResponse接口setStatus下断点就行，
其他响应一样操作。
```

<!-- 这是一张图片，ocr 内容为：6 拖拽至此上传 简体中文 下载 好视通 视频会议企业版 服务器管理后台 用户名 密码 Y M45T 验证码 记住空码 登录 可............................................X 中网络 周六器 性能 司 无障姆环境 仔铛 源应用程序 小节流:兴 有  MIM (55 55 58 54 照级 W5 甘他  头 COOKIE 请求 响应 机时 安全性 状合 成名 文件 方法 大厦 传偷 8 192168.1.146:8443  W GET VALIDATECODE DO/TIMESTAMP-1780020674382 宁过法消息头 200 招商业发 IMG FUD GET HTTPS//192.1.1.146:8443/LOGIN/VALIDATECODE.DO7TIMESTAMP:1780820674382 200  R) -->
![](https://xinyisleep.github.io/img/2026/好视通/1.png)

<!-- 这是一张图片，ocr 内容为：LOGINCONTROLLER IF (NULL !- ENABLEDPWD){ 906 907 UTE("ENABLEDPWD", ENABLEDPWD); SESSION,SETATTRIBUTE("EN 910 (NODEINFO NUII) ( NUII SESSION.SETATTRIBUTE("USERNODENAME", NODEINFO.GETNODENAME()); 911 912 SESSION.SETATTRIBUTE("USERNODEID",NODEINFO-GETNODEID()); SESSION.SETATTRIBUTE("NODEID",NODEINFO.GETNODEID(); 913 SESSION.SETATTRIBUTE("USERNAME", USERINFO.GETUSERNAME()); 916 SESSION.SETATTRIBUTE("USERID", USERINFO.GETUSERID()+ "); 917 918 SESSION.SETATTRIBUTE("DEPARTID", USERINFO-GETDEPARTID() + ""); 919 SESSION.SETATTRIBUTE("PWD",USERINFO.GETPASSWORD()); 922 SESSION.SETATTRIBUTE("CLIENTIP", CLIENTIPVALIDATE.GETIPADDR(REQUEST)); 925 SESSION.SETATTRIBUTE("WEBSERVICE", WEBSERVICESIZE); SESSION.SETATTRIBUTE(FSPPAASCONSTS.TOKEN, TOKENMANMANAGER -GETTOKENFORSESSION(SESSION)); 927 INTEGER.VALUEOF(THIS.LICENSEINFOSERVICENEW-GETLICENSETYPE()); 929 SESSION.SETATTRIBUTE("LICENSETYPE",INTEGER @REQUESTMAPPING(L" 'VALIDATECODE.DO"}) PUBLAST, HOPSSTRIDSY OSJECT) VATECODE(UTEPSE) FARVIETREQUEST REQUEST, HTTPSERUJETRESPANSE) THRONS EXC 937 938 RESPONSE .SETCONTENTTYPE(RECORDCONSTS.BASE64 PNG); ("PRAGMA", "NO-CACHE"); 940 RESPONSE.SETHEADER RESPONSE .SETHEADER("CACHE-CONTROL","NO-CACHE"); 941 942 RESPONSE.SETDATEHEADER("EXPIRES", 0L);  GETSESSION(); 943 SESSION SESSION DE VALIDATECODE - NEW VALIDATECODE(155, 40, 4, 80); 945 VALIDATECODE VAL 946 VALIDATECODE.WRITE((OUTPUTSTREAM) RESPONSE-GETOUTPUTSTREAM()); SESSION.SETATTRIBUTE("CODE",VALIDATECODE GETCODE()); 947 949 RETURN NULL; 了 956 PRIVATE LIST<SESSION> EFFECTIVESSIONLIST(){ COLLECTION<SESSION> COLLSESSION - THIS.SESSIONDAO.GETACTIVESSIONS(); 957 958 LIST<SESSION> LISTSESSION - NEW ARRAYLIST<>(); FOR (SESSION CURRSESSION : COLLSESSION){ (CURRSESSION.GETATTRIBUTE("USERNAME") !- NULL) { 961 LISTSESSION.ADD(CURRSESSION); 962 RETURN LISTSESSION; 965 -->
![](https://xinyisleep.github.io/img/2026/好视通/2.png)

<!-- 这是一张图片，ocr 内容为：互 二 R REFERERFILTER.CLASS APPLICATIONFILTERCHAIN.CLASS FACEINFOCONTROLLER.CLASS SPRINGSERVLET.XML WEBXML 反编译:CLASS文件,字节码版本:52.0(JAVA 8) 下载 选择源( COMMONS CON @CONTRACT(PURE TRUE) PUBLIC REFERFILTER() COMMONS-DBC 36 厂 COMMONS DISC 37 COMMONS-FILEL 38 COMMONS FILED 0个用法 COMMONS-HTTE PTION, SERVLETEXCEPT 39 HTTPSERVLETREQUEST REQUEST - (HTTPSERVLETREQUEST)SERVLETREQUEST; STRING REFERER ; REQUEST.GETHEADER( S: "REFER"); PREDICATE<STRING> PREDICATE : (C) -> { .CONTAINSIGNORECASE(REFER, C); RETURN STRINGUTILS.EQUALSIGNORECASE(REFERER, C) LL STRINGUTILS.CONTE 卜; BOOLEAN ISPASS - THIS.GETPASSLIST(REQUEST).STREAM().ANYMATCH(PREDICATE); IF (ISPASS){ FILTERCHAIN.DOFILTER(SERVLETREQUEST, SERVLETRESPONSE); ELSEF DOM4I-1.6.1-1. LOG.INFO("SUSPECTED CSRF ATTACK, REFERER IS [FH]", REFERER); EASYEXCEL-2.1. 国 ECJ-4.15JAR 50 L EHCACHE-3.1. 51 国未命名 调试: 质祝证券量 对农达式求值(ENTER)或添加监视(CTRL+SHIFT+ENTER) 'CATALINA-EXEC-12'@20,907在组MAIN"止在运行 JAVA DOFILTER:166,APPLICATIONFILTERCHAIN(ORG.APACHE.CATALINA.CORE) I THIS                                                                                                O SERVLETREQUEST - (SHIROHTTPSERVLETREQUEST@ 20910) DOFILTER.53.WSFILTER (ORG.APACHE.TOMCAT.WEBSOCKET.SERVER) SERVLETRESPONSE - (SHIROHTTPSERVLETRESPONSE@20911) INTERNALDOFILTER:193,APPLICATIONFILTERCHAIN (ORG.APACHE.CATALINA.CORE) FILTERCHAIN(APPLICATIONFILTERCHAIN@20913) DOFILTER.166.APPLICATIONFILTERCHAIN (ORG.APACHE.CATALINA.CORE) REQUEST -(SHIROHTTPSERVLETREQUEST@20910) DOFILTER:45,REFERERFILTER(COM.HST.CES.FILTER) *HTTPS://192.168.1.146:8443/LOGIN/ADMINTOLOGIN.DO* ROFERER [REFERERFILTER$LAMBDA@20915] PREDICATE DOFILTER:166,APPLICATIONFILTERCHAIN (ORG.APACHE.CATALINA.CORE) ISPASSTRUE DOFILTER:39.JQUERYAJAXCACHETOKENFILTER (COM.HSE.CES.FILTED PPLICATIONFILTERCHAIN (ORG.APACHE.CATALINA.CORE) FILTERCHAIN(ORG.APACHE.CATALINA.CORE) 南 DOFILTER56,DOWNLOADFILTER (COM.HST.CES.FILTER) 使用CH+AL+向上普头和CHL-ALL-向下箭头从IDE中的任重切换板 -->
![](https://xinyisleep.github.io/img/2026/好视通/3.png)

```
这里我们还是先手动看看项目吧，代码一中可以看到这是springmvc的项目，第一个过滤器filter是shiro的我们先看看是
不是用到了鉴权方面直接filterChainDefinitions代码二，这是弄了个啥呀所以这里一定不是鉴权的filter过滤器，因为没看到
authc所以这里可以过了，再看看代码一中第二个过滤器，DownloadFilter。
```

```java
	<servlet>
		<description>spring mvc servlet</description>
		<servlet-name>springMvc</servlet-name>
		<servlet-class>org.springframework.web.servlet.DispatcherServlet</servlet-class>
		<init-param>
			<description>spring mvc 配置文件</description>
			<param-name>contextConfigLocation</param-name>
			<param-value>classpath*:conf/spring/springServlet.xml</param-value>
		</init-param>
		<load-on-startup>1</load-on-startup>
	</servlet>
	<servlet-mapping>
		<servlet-name>springMvc</servlet-name>
		<url-pattern>/</url-pattern>
	</servlet-mapping>
	<filter>
		<filter-name>shiroFilter</filter-name>
		<filter-class>org.springframework.web.filter.DelegatingFilterProxy</filter-class>
		<init-param>
			<param-name>targetFilterLifecycle</param-name>
			<param-value>true</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>shiroFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
	<filter>
		<filter-name>DownloadFilter</filter-name>
		<filter-class>com.hst.ces.filter.DownloadFilter</filter-class>
		<init-param>
			<param-name>filterUrl</param-name>
			<param-value>download,filename</param-value>
		</init-param>
	</filter>
	<filter-mapping>
		<filter-name>DownloadFilter</filter-name>
		<url-pattern>/*</url-pattern>
	</filter-mapping>
```

```java
   	<!-- Shiro Filter -->
	<bean id="shiroFilter" class="org.apache.shiro.spring.web.ShiroFilterFactoryBean">
		<property name="securityManager" ref="securityManager" />
		<property name="loginUrl" value="/login/toLogin.do" />
		<property name="successUrl" value="/index/index" />
		<!-- <property name="unauthorizedUrl" value="/login_toLogin" /> -->
		
		<!-- <property name="filters">
            <map>
                <entry key="kickout" value-ref="kickoutSessionControlFilter"/> 
            </map>
        </property> -->
       
		<property name="filterChainDefinitions">
			<value>
	           	/resources/fmWeb/**		=anon
	           	/resources/plugin/**	=anon
	           	/resources/skin/**		=anon
	           	/resources/**			=anon
	           	<!--  /login/login.do = kickout -->
			</value>
		</property>
	</bean>
```

```
代码一这个就有点小说法了，首先先走入了needCheck进去看看代码二,其中获取uri转换成小写来匹配是否
存在private String[] urlArray = new String[]{"download", "file"};这两个参数的，存在之后进入代码一，
String[] var9 = (String[])parameterMap.get(key);获取请求参数内容出现../就403，查阅了一下发现是对历史漏洞的一个修复。
```

```java
public void doFilter(ServletRequest servletRequest, ServletResponse servletResponse, FilterChain chain) throws IOException, ServletException {
        HttpServletRequest request = (HttpServletRequest)servletRequest;
        HttpServletResponse response = (HttpServletResponse)servletResponse;
        if (this.needCheck(request)) {
            Map<String, String[]> parameterMap = request.getParameterMap();
            Iterator var7 = parameterMap.keySet().iterator();

            while(var7.hasNext()) {
                String key = (String)var7.next();
                String[] var9 = (String[])parameterMap.get(key);
                int var10 = var9.length;

                for(int var11 = 0; var11 < var10; ++var11) {
                    String value = var9[var11];
                    if (value.contains("../")) {
                        response.setStatus(403);
                        PrintWriter out = response.getWriter();

                        try {
                            out.println("Illegal file path \"" + value + "\"");
                        } finally {
                            if (Collections.singletonList(out).get(0) != null) {
                                out.close();
                            }

                        }

                        return;
                    }
                }
            }
        }
```

```java
private String[] urlArray = new String[]{"download", "file"};
private boolean needCheck(HttpServletRequest request) {
        String url = request.getRequestURI();
        String[] var3 = this.urlArray;
        int var4 = var3.length;

        for(int var5 = 0; var5 < var4; ++var5) {
            String filterUrl = var3[var5];
            if (url.toLowerCase().contains(filterUrl)) {
                return true;
            }
        }

        return false;
    }
```

```
到这里都没有发现真正的鉴权在哪说明在springmvc配置文件中，存在四个拦截器代码一,这里第一个并不是，
第2个看名字应该都知道了TokenExistInterceptor进入看看，图一中是通过session拿去token存在内容就是true,
那么很明显我们并不能修改token的内容，所以们办法绕过(不一样比如放行路由中存在设置token的就绕过了)。
```

```java
 <mvc:interceptors>
  	    <!-- 实现本地化信息的监听（来实现url参数动态指定locale）,V4.4解决安全问题，把切换语言的方法改到后台。
        <bean id="localeChangeInterceptor" class="org.springframework.web.servlet.i18n.LocaleChangeInterceptor">
        	<property name="paramName" value="lang" />
        </bean> -->
       <mvc:interceptor>
            <mvc:mapping path="/**/*.do" />
            <!-- V4.7 解决会话固定的安全问题带出来的极端情况，防止有用户在登录的过程中，其它用户直接访问login/toIndex.do能登录成功。 -->
            <!-- login下除了/toIndex.do、/tab.do、/logout.do放行，其它请求要经过拦截器 -->
            <mvc:exclude-mapping path="/login/*ogin.do" />
			<mvc:exclude-mapping path="/login/to*Error.do" />
			<mvc:exclude-mapping path="/login/validateCode.do" />
			<mvc:exclude-mapping path="/login/getResourcePlatformAddr.do" />
			<mvc:exclude-mapping path="/login/setLanguage.do" />
			<mvc:exclude-mapping path="/login/createQRCode.do" />
			<mvc:exclude-mapping path="/login/clientDownload.do" />
            <mvc:exclude-mapping path="/launch/**" />
            <!-- V4.3 不能通过链接直接访问，解决该链接跨站点请求伪造问题[IE8浏览器不兼容,先放行] -->
            <mvc:exclude-mapping path="/register/**" />
            <mvc:exclude-mapping path="/license/**" />
            <!-- 以下三条针对IE浏览器播放视频监控不到http referer特意放行 -->
            <mvc:exclude-mapping path="/record/play.do"/>
            <mvc:exclude-mapping path="/record/playBase.do"/>
            <mvc:exclude-mapping path="/record/downloadRecord.do"/>
            <!-- 放行统一认证服务 -->
            <mvc:exclude-mapping path="/oauth2/token"/>
            <mvc:exclude-mapping path="/v1/tokeninfo"/>
		   <!-- 放行级联调用的清除缓存 -->
		   <mvc:exclude-mapping path="/cache/clearAllCache"/>
            <bean class="com.hst.ces.interceptor.UrlLegalInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**/*.do" />
            <mvc:exclude-mapping path="/login/**" />
            <mvc:exclude-mapping path="/launch/**" />
            <mvc:exclude-mapping path="/register/**" />
            <mvc:exclude-mapping path="/license/**" />
            <!-- 放行统一认证服务 -->
            <mvc:exclude-mapping path="/oauth2/token"/>
            <mvc:exclude-mapping path="/v1/tokeninfo"/>
            <!-- 放行人脸识别-->
            <mvc:exclude-mapping path="/face/validRequest.do"/>
            <mvc:exclude-mapping path="/face/uploadFace.do"/>
			<!-- 放行级联调用的清除缓存 -->
			<mvc:exclude-mapping path="/cache/clearAllCache"/>
            <bean class="com.hst.ces.interceptor.TokenExistInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
            <mvc:mapping path="/**/*.do" />
            <mvc:exclude-mapping path="/login/**" />
            <mvc:exclude-mapping path="/launch/**" />
            <mvc:exclude-mapping path="/register/**" />
            <mvc:exclude-mapping path="/license/**" />
            <!-- 放行统一认证服务 -->
            <mvc:exclude-mapping path="/oauth2/token"/>
            <mvc:exclude-mapping path="/v1/tokeninfo"/>
            <!-- 放行人脸识别-->
            <mvc:exclude-mapping path="/face/validRequest.do"/>
            <mvc:exclude-mapping path="/face/uploadFace.do"/>
			<!-- 放行级联调用的清除缓存 -->
			<mvc:exclude-mapping path="/cache/clearAllCache"/>
            <bean class="com.hst.ces.interceptor.SessionTimeoutInterceptor"/>
        </mvc:interceptor>
        <mvc:interceptor>
			<mvc:mapping path="/**/*.do*"/>
			<mvc:exclude-mapping path="/login/**" />
			<mvc:exclude-mapping path="/launch/**" />
			<mvc:exclude-mapping path="/register/**" />
			<mvc:exclude-mapping path="/license/**" />
			<mvc:exclude-mapping path="/user/*ditSelf.do" />
			<!-- 放行统一认证服务 -->
            <mvc:exclude-mapping path="/oauth2/token"/>
            <mvc:exclude-mapping path="/v1/tokeninfo"/>
            <!-- 放行人脸识别-->
            <mvc:exclude-mapping path="/face/validRequest.do"/>
            <mvc:exclude-mapping path="/face/uploadFace.do"/>
			<!-- 放行级联调用的清除缓存 -->
			<mvc:exclude-mapping path="/cache/clearAllCache"/>
			<bean class="com.hst.ces.interceptor.RightsHandlerInterceptor"/>
		</mvc:interceptor>
		 
    </mvc:interceptors>
```

<!-- 这是一张图片，ocr 内容为：用法 SUBJECT SUBJECT - SECURITYUTILS.GETSUBJECT(); SESSION SESSION 三 SUBJECT.GETSESSION(); STRING TOKEN - (STRING)SESSION GETATTRIBUTE( O:"TOKEN'); IF (NUL F (NULL !: TOKEN && TOKEN !: "") { TRUE; RETURN ELSE  RESPONSE.SETSTATUS(403);  RESPONSE.SETHEADER( S:"REFUSED", S1:"TRUE"); REQUEST.GETREQUESTURL(); STRINGBUFFER URL  STRING CONTEXTURL 三 LE 1 - URL.DELETE(URL.LENGTHO) - REQUEST.GETREQUESTURIO.LENGTHO, URL.LENGTHO),TOSTRINGO; IF (REQUEST.GETCONTEXTPATHO !- NULL G& !NW.EQUALS(REQUEST.GETCONTEXTPATH() { CONTEXTURL 3 CONTEXTURL + REQUEST.GETCONTEXTPATH(); RESPONSE.SENDREDIRECT( S: CONTEXTURL + "/LOGIN/TOLOGIN.DO"); N FALSE; RETURN -->
![](https://xinyisleep.github.io/img/2026/好视通/4.png)


<h1 id="mWxxssssc">三.开始审计</h1>

<h2 id="MBwwwddd93N">3.1: 任意文件下载</h2>
```
上面我们得知token是从session拿的所以这里没办法绕过，现在的思路就是在不鉴权的路由中发现漏洞，
因为存在三个拦截器并且都是拦截的所有<mvc:mapping path="/**/*.do" />，那么就得找这四个中都存在放行的，
第一个不用看我们看后面三个<mvc:exclude-mapping path="/register/**" />我们找找有没有什么安全问题代码一，
其中download明显是一个文件下载漏洞，但是不要忘记了之前的filter是存在修复，但是这里依旧可以绕过。
```

<!-- 这是一张图片，ocr 内容为：@REQUESTMAPPING({O~"/TODOWNLOAD.DO"}) 187 188 PUBLIC VOID DOWNLOAD(STRING FILENAME, HTTPSERVLETRESPONSE RESPONSE)  SESSION SESSION ; THIS.GETSESSION(); 189 190 191 TRY { IF (STRINGUTILS.ISBLANK(FILENAME)) { 192 "REGISTER.DAT"; FILENAME E                                                                   193 子 194 195 STRING FILEPATH - CONFIGMGR.GETINSTANCE().GETDOWNLOADPATH(); 196 STRING COMPLETEPATH - FILEPATH + FILE.SEPARATOR + FILENAME; 197 FILE FILE ; NEW FILE(COMPLETEPATH); 198  IF (FILE.EXISTS()) { 199 "CONTENT-DISPOSITION", S1:"ATTACHMENT;FILENAME:" + URLENCODER.ENCODE(FILENAME" 200 RESPONSE.SETHEADER( BYTE[] BUFFER BYTE[1024]; 201 NEW 202 TRY 卡 203 FILEINPUTSTREAM FIS - NEW FILEINPUTSTREAM(FILE); 204 THROWABLE VAR9 ; NULL; 205 206 TRY 卡 207 OUTPUTSTREAM OUT - RESPONSE.GETOUTPUTSTREAM(); 208  THROWABLE VAR11 ; NULL; 209 210 211 TRY INT LEN 三 FALSE; 212 213 INT LEN; 214  WHILE((LEN : FIS.READ(BUFFER)) > 0) { 215  OUT.WRITE(BUFFER, OFF:0, LEN); 216 217 -->
![](https://xinyisleep.github.io/img/2026/好视通/5.png)

```
之前只是过滤了../但是不要忘记这个是window的程序呀通过..\就绕过了，下面图一。
```

<!-- 这是一张图片，ocr 内容为：GET /REGISTER/TODOWNLOAD.DO?FILENAME HTTP/1.1 200 .. L.. L.. 1..1.. 1..1..1.. |WINDOWS\WIN.INI HTTP/1.1 STRICT-TRANSPORT-SECURITY:MAX-AGE-0 X-FRAME-OPTIONS:SAMEORIGIN HOST:192.168.1.146:8443 X-CONTENT-TYPE-0PTIONS:NOSNIFF COOKIE: MLDN-SESSION-ID-E834527F-417F-411A-B2E3-D744974EB956 USER-AGENT:MOZILLA/5.0 (WINDOWS NT 10.0:WIN64:X64:X64:RV:151.0) X-XSS-PROTECTION:1;MODEBLOCK GECKO/20100101 FIREFOX/151.0 VARY: ORIGIN ACCEPT: TEXT/HTML,APPLICATION/XHTML+XML,APPLICATION/XML:G-0.9.*/*:Q-0.8 CONTENT-DI SPOSITION ATTACHMENT:FILENAME二,%5C.%5C.%5C..%5C..%5C.%5C.%5C.%5CWINDOWS%50WS%50WIN. ACCEPT-LANGUAGE: ZH-CN,ZH;Q-0.9,ZH-TW:Q-0.8,ZH-HK:Q-0,7,EN-US:Q-0.6,EN:Q-0,5 INI ACCEPT-ENCODING:GZIP,DEFLATE,BR CONTENT-LENGTH:92 DATE:SUN. 07 JUN 2026 09:03:35 GMT REFERER: HTTPS://192.168.1.146:8443/ UPGRADE-INSECURE-REQUESTS: KEEP-ALIVE :TIMEOUT20 10 SEC-FETCH-DEST:DOCUMENT CONNECTION:KEEP-ALIVE 11 VINH SEC-FETCH-MODE:NAVIGATE SERVER:UNKNOWN SEC-FETCH-SITE: SAME-OR IGIN SEC-FETCH-USER:?1 FOR 16-BIT APP SUPPORT PRIORITY:U [FONTS] TE:TRAILERS 15 [EXTENSIONS] 16 CONNECTION:KEEP-ALLIYE 17 [MCI EXTENSIONS] [FILES] 18 [MAIL] 20 -->
![](https://xinyisleep.github.io/img/2026/好视通/6.png)


<h2 id="MBdfffffdd93N">3.2：添加管理员</h2>
```
挺奇葩的只能说，项目存在多个微服务其中存在wsdl,不知道怎么看微服务访问的话可以看一下\tomcat\conf\server.xml
文件图一,接着看这个微服务的web.xml其中发现了org.codehaus.xfire.transport.http.XFireConfigurableServlet。
```

<!-- 这是一张图片，ocr 内容为：SERVER.XML-记事本 文件() 编辑(日)格式(O)  查看(V) 帮助(H) <!-- USE THE LOCKOUTREALM TO PREVENT ATTEMPTS TO GUESS USER PASSWORDS VIA A BRUTE-FORCE ATTACK--> <REALM CLASSNAME-'ORG.APACHE.CATALINA-REALM.LOCKOUTREALM'> 2!-- THIS REALM USES THE USERDATABASE CONFIQURED IN THE GLOBAL JNDI RESOURCES UNDER THE KEY "USERDATABASE",ANY EDITS THAT ARE PERFORMED AGAINST THIS USERDATABASE ARE IMMEDIATELY AVAILABLE FOR USE BY THE REALM.-->> CREALM CLASSNAME三"ORG:APACHE.CATALINAREALM,USERDATABASEREALM" RESOURCENAME三"USERDATABASE"/> </REALM> NAME-"LOCALHOST" UNPACKWARS-"TRUE" DEPLOYONSTARTUP-"FALSE"> HOST APPBASE,,WEBAPPS,AUTODEPLOY-,FALSE"NAME:"LOCA :---SINGLESIGNON VALVE,SHARE AUTHENTICATION BETWEEN WEB APPLICATIONS DOCUMENTATION AT://DOCS/CONFIG/VALVE.HTML --> -I> <VALVE CLASSNAME " "ORG.APACHE.CATALINA.AUTHENTICATOR.SINGLESIGNON"/ <!- ACCESS LOG PROCESSES ALL EXAMPLE. DOCUMENTATION AT://DOCS/CONFIG/VALVE.HTML NOTE:THE PATTERN USED IS EQUIVALENT TO USING PATTERN-"COMMON'- <VANE CLASSNAME''ORG-APACHECATAFINAVAVAVES ERRERURTS ERURLVAVE' SHOWREPORTS FALS FALS  FALS  FALSE"/ VALVE' DRECTORY- LOGS"PATTERN二"%H %U %U AT  &QUOT %6R&QUOT;%B'PREFIX- LOCALHOST ACCESS LOG'SUFFX- "TA VALVE CLASSNAME-"ORG.APACHE.CATALINA.VALVES.ACCESSLOGVALVE"S <CONTEXT PATH-"" DOCBASE "FMWEB" RELOADABLE CROSSCONTEXT-TRUE RALSE CONTEXT PATH "/AUTHSERVER"DOCBA AUTHSERVER RELOADABLE CROSSCONTEXT"TRUE <CONTEXT PATH-"/FMAPI" DOCB: "FMAPI"RELOADABLE "FALSE SCONTEXT "TRUE"/> 'FALSE' CROSSCONTEXT-'TRUE"/> <CONTEXT PATH"/GTONESERVICES3.5" DOCBASE-"GTONES GTONESERVICES3.5 RELOADABLE-"FA  CCONTEXT PATH:"/MEEFING-SENER RELOABLER" DOCBASE;  TALE_  TASOURCE-SENER RELOADABLE_ COSSCONTEXT / </HOST> </ENGINE> </SERVICE> </SERVER> -->
![](https://xinyisleep.github.io/img/2026/好视通/7.png)

```
接着看配置文件这里我们直接看实现图一，在图二中可以看到直接可以添加管理员用户，但是这里存在一个问题就是再登录的时候会验证
dvo.getDepartID()必须是0才代表管理员用户，但是在图二中是通过查询了数据库表里面的内容来当做dvo.getDepartID()的并不能控制，
那么再找找其他方法发现AllDepart方法。

```

<!-- 这是一张图片，ocr 内容为：INDEX.JSP SERVICESXML WEB.XML ' ENCODING-"UTF-8"? <?XML VERSION:"1.0" EN S XMLNS-"HTTP://XFIRE.CODEHAUS.ORG/CONFIG/1.0"> <BEANS ) 3 <SERVICE> 4 <NAME>GTONEWEBSERVICES</NAME> <SERVICECLASS> COM.GTONE3.WEBSERVICE.DBUPDATE.IGTONEWEBSERVICES </SERVICECLASS> 白白 <IMPLEMENTATIONCLASS> 10 COM.GTONE3.WEBSERVICE.DBUPDATE.GTONEWEBSERVICESIMPL 11  </IMPLEMENTATIONCLASS> 12 <STYLE>WRAPPED</STYLE> 13 <USE>LITERAL</USE> 14 <SCOPE>APPLICATION</SCOPE> </SERVICE> 15 16 17 <SERVICE> <NAME>NEWSERVICES</NAME> <SERVICECLASS> COM.GTONE3.WEBSERVICE.DBUPDATE.INEWSERVICES 20 </SERVICECLASS> 22 <IMPLEMENTATIONCLASS> COM.GTONE3.WEBSERVICE.DBUPDATE.NEWSERVICESIMPL 23 </IMPLEMENTATIONCLASS> 24 <STYLE>WRAPPED</STYLE> 25 <USE>LITERAL</USE> 26 27 <SCOPE>APPLICATION</SCOPE> </SERVICE> 28 29 /BEANS> -->
![](https://xinyisleep.github.io/img/2026/好视通/8.png)

<!-- 这是一张图片，ocr 内容为：PUBLICINTUSER G TELEPHONE, STR NEW NODEBO();  USERVO VO ; NEW USERVO(); 55 56 NEW DEPARTVO(); 57 ).QUERYDEPARTIDBYBACKUPDEPARTID(DEPARTID, NODEID); DEPARTVO DVO DEPARTB0.GETINSTANCE() 58 IF (DVO.GETDEPARTID() - 0) SGE RETURN 3. 9446806608000071164 ELSE F VO.SETUSERNAME(USERNAME)1  VO.SETPASSWORD(PASSWORD); VO.SETDISPLAYNAME(DISPLAYNAME); VO.SETSEX(SEX); VO.SETSTATUS(STATUS); VO.SETMOBILE(MOBILE); VO.SETTELEPHONE(TELEPHONE);  VO.SETEMAIL(EMAIL); VO.SETUSERLEVEL(USERLEVEL); VO.SETDEPARTID(DVO.GETDEPARTID(); VO.SETNODEID(NODEID);  VO.SETADMINROLE(ADMINROLE); I - USERBO.GETINSTANCE().ISEXISTUSER(USERNAME, NODEID); INT IRESULT 三 TRUE; INT INT IRESULT; 76 IF(I>0){ - HEANDN AATTNETANAA() LINDATA(UA), -->
![](https://xinyisleep.github.io/img/2026/好视通/9.png)

```
可以看到图一存在两个参数，第一个参数存在值的时候就会去遍历这里完全没必要只需要不传入内容为空就可跳过这段代码，
那么我们就来到了图二，这里虽然我们也是通过查询出来的结果作为getDepartID代码一，但是我们可以穿一个vo.getDepartID()
这个id为0查询不到返回的就是0刚好写入插入，那么要走到else语句里面我们就需要代码2小于于0，代码其实就是查询管理员表
getNodeID对应的id查到了返回肯定大于1就会进入更新，小于零及时插入穿一个不存在的id就行了。
```

```java
DepartVO dvo = DepartBO.getInstance().QueryDepartIDByBackUpDepartID(vo.getDepartID(), vo.getNodeID());
```

```java
count = DepartBO.getInstance().DepartIDByDepartID(vo.getDepartID(), vo.getNodeID());
```

<!-- 这是一张图片，ocr 内容为：(1) THROWS EXCEPTION 1384 PUBLIC INT ALLDEPART(COLLECTION<DEPARTVO> COLL, COLLECTION<USERVO> COLL1) T INT COUNTO 0 1385  ITERATOR IT - COLL.ITERATOR(); 1386 DEPARTVO[] COLL1 NEW DEPARTVO[COLL.SIZE()] 1387  STRING NODEID 三 NULL; 1388 STRING NODEID1 ; NULL; 1389 1390 INT I                                                                                                 INT J 三 0; 1391 INT N                                                                                                 1392 1393 WHILE(TRUE) { 1394 INT COUNT1; 1395 甲 WHILE(IT.HASNEXT()) 1396 1495 IF (COUNTO ! COLL.SIZE()) 1496 RETURN -1; 1497 1498 1499 INT IRESULT - TRUE; 1500 COUNT1  0: 1501 ITERATOR USERIT ; COLLLL.ITERATOR(); 1502 USERVO[] USERCOLLL - NEW USERVO[COLLLL.SIZE()]; 1503  NULL; 1504 STRING NODEID2 EW STRING[10];  STRING[] A - NEW S 1505 INT J1 1506 日; 1507 I1 INT 1508 INT M; 1509 INT RESULTO; 1510  WHILE(USERIT.HASNEXT()) { 1511 1512 NEW USERVO(; -->
![](https://xinyisleep.github.io/img/2026/好视通/10.png)

<!-- 这是一张图片，ocr 内容为：1537 VO.GETNODEID(); USERB0.GETI ).GETINSTANCE().ISEXISTUSER(VO.GETUSERNAME() IF (M>0) 1538 USERVO UVO 三 NEW USERVO(); 1539  UVO.SETUSERNAME(VO.GETUSERNAME(); 1540 UVO.SETPASSWORD(VO.GETPASSWORD()); 1541 UVO.SETDISPLAYNAME(VO.GETDISPLAYNAME()); 1542  UVO.SETSEX(VO.GETSEX()); 1543 UVO.SETSTATUS(VO.GETSTATUS(); 1544 UVO.SETMOBILE(VO.GETMOBILE()); 1545 UVO.SETTELEPHONE(VO.GETTELEPHONE()); 1546  UVO.SETEMAIL(VO.GETEMAIL(); 1547 UVO.SETUSERLEVEL(VO.GETUSERLEVEL()); 1548 UVO.SETDEPARTID(COUNT); 1549 UVO.SETNODEID(VO.GETNODEID(); 1550 UVO.SETADMINROLE(VO.GETADMINROLE()); 1551 USERB0.GETINSTANCE().UPDATE(UVO); 1552 ++COUNT1: 1553 1554 ELSEF 1555 NEW DEPARTVO();  USERVO IVO 三 NEW USERVO(); 1556 1557 DEPARTVO DVO - DEPARTBO-GETINSTANCE(),QUERYDEPARTIDEYBACKUPPEPPEPARTID(VO-GETDEPARTIDO, VO-GETNODEIDO 1558 IVO.SETUSERNAME(VO.GETUSERNAME(); IVO.SETPASSWORD(VO.GETPASSWORD()); 1559 IVO.SETDISPLAYNAME(VO.GETDISPLAYNAME()); 1560  IVO.SETSEX(VO.GETSEX()); 1561 1562 IVO.SETSTATUS(VO.GETSTATUS()); IVO.SETMOBILE(VO.GETMOBILE()); 1563 IVO.SETTELEPHONE(VO.GETTELEPHONE()); 1564  IVO.SETEMAIL(VO.GETEMAIL(); 1565 IVO.SETUSERLEVEL(VO.GETUSERLEV 1566  IVO.SETDEPARTID(DVO.GETDEPARTID()); 1567  IVO.SETNODEID(VO.GETNODEIDO); 1568 -->
![](https://xinyisleep.github.io/img/2026/好视通/11.png)

```
其实这里并不是通用的，因为其中会传入一个vo.getNodeID()这个id虽然在数据库中但是并不是固定的五位数字，
因为在登录的时候需要验证这个数字，当然可以直接爆破但是实战下还是。。。。
```

